Adelina:
need to enter her name and id

Both of us:
need to create a tester and start to check the existing functions, and debugs the code
we need to focus on the AVLNode class and make sure it's Done :)
we should start with debugging AVLNOde: setSize(), GetHeight(), setBalanceFactor() by this order
